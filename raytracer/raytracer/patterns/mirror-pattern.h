#pragma once

#include "patterns/pattern.h"
#include "math/point.h"


namespace patterns
{
    Pattern2D mirror(Pattern2D child);
    Pattern2D mirror_x(Pattern2D child);
    Pattern2D mirror_y(Pattern2D child);
}
